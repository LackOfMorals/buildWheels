version: 2

project_name: buildwheels

before:
  hooks:
    - go mod tidy
    - go test -count=1 ./...

builds:
  - id: buildwheels
    main: ./
    binary: buildwheels
    env:
      - CGO_ENABLED=0        # pure Go, no C deps — enables clean cross-compilation
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    # Exclude combos you don't want (e.g. Windows ARM)
    ignore:
      - goos: windows
        goarch: arm64
    ldflags:
      - -X "main.Version={{.Tag}}"

archives:
  - id: default
  formats:
      - tar.gz
  # this name template makes the OS and Arch compatible with the results of `uname`.
  name_template: >-
      {{ .Binary }}_
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    # .zip for Windows, .tar.gz for everything else
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - README.md
      - LICENSE.txt

checksum:
  name_template: checksums.txt

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - Merge pull request
      - Merge branch